# AGENT — ui-3d module (FROZEN)

This module is **frozen in v1** per D-004. Same rules as `ui-web`.

## Status

**Frozen**. No new features. Bug fixes only.

## Purpose

Three.js 3D web front-end. Built with Vite + TS + three. Deployed to the
same gh-pages site as ui-web, at `/3d/`.

## Why frozen

Same as ui-web. v1 focuses on UE.

## When to unfreeze

After v1 UE ships and there's appetite for a web 3D alternative. Probably
v2 territory.

## Allowed tasks

Same as ui-web:
- Build fixes
- API contract sync with `dealer-ai`

## Escalation triggers

Same as ui-web.
