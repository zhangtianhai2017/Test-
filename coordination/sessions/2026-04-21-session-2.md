# Session — 2026-04-21 #2 — v1 close

## Role
PM under D-012. This session ran from just after M1 completion through
the entire v1 close (M1–M13).

## What happened

M4 completed in 4 micro-tasks with the NPC driver landing a 1-human-+-
2-NPCs WS integration test at the end.

M9 extended dealer-ai with 9 new psychological-layer event types +
GameState fields; ran in parallel with M4b (first confirmed parallel
dispatch under D-024).

M5 shipped in four parts (a / b.1 / b.2a / b.2b / b.2c after one
stream-idle timeout on the first monolithic attempt). Result: a full
UE WebSocket client subsystem with 24 BP delegates and 16 send
methods, source-complete, never compiled in the sandbox.

M10 added CosyVoice 2 TTS integration path to dealer-ai: CPU mode
falls back cleanly, GPU mode attempts the real load. Two new HTTP
endpoints (/session/{id}/tts, /session/{id}/quip+tts) plus /voices.

M12 wired NPC tilt into the server's ai-npc usage via a per-table
Map<seatIndex, number> rather than mutating engine state (getState
returns deep copies), plus dealer-state evolution in dealer-ai.
Ran in parallel with M5b.2c.

M6 created ABlackjackNetTableActor, the scene driver that binds to
14 UE client delegates.

M11 added UBlackjackAudioClient, the TTS playback subsystem.

M7 split into M7a (heat/morale subsystems + pit boss actor) and
M7b (decision timer actor + gesture input library). Both shipped,
parallel with M11.

M8 added tell + trust subsystems + perception wiring. Parallel with
M13.

M13 added the Windows packaging scripts + DEPLOY_V1.md runbook.

## Parallel dispatch experience

Under D-024 (up to 3 subagents in parallel, distinct modules / distinct
files): ran at least 8 concurrent pairs. Zero file conflicts encountered
in those that stayed disciplined. One commit history smudge when an
early `git add packages/ue-plugin/` picked up a half-written M11 file
during the M6 commit — fixed the next round by `git add <specific-files>`
rather than `git add <dir>`.

## Decisions added this session

D-023 seat() helper uses activeSeatIndex (resolves Q-001)
D-024 PM parallel dispatch up to 3 subagents

## Tests

- engine: 57 pass
- ai-npc: 93 pass
- game-server: 64 pass
- dealer-ai: 17 pass
- Total: 231 automated tests

UE client: source-complete, never compiled here. Validation awaits
user's Windows + UE 5.3 toolchain.

## Commits this session

M1 chain: 196438f, 1f69a09, 6cf9a68, a9059fd, fb80efb, 3f52257,
          2f5b9aa, 085f46e, 62198ba, f835cbc
M2 chain: 64433ea, dcf37d8, 44f8df3, 134209a, 664e768, 1f0f988,
          7dde0e1
M3 chain: 3122c1d, 89032d4, 4c76ae2, 6b033d7, e1a2cc8
M4 chain: 8d196ab (bridge), 762f07e (human actions), 3a0cc44 (NPC)
M5 chain: 4871ebd (skeleton), c241609 (senders), ca39edc (enums),
          a1c6b1a (structs), 3cde396 (delegates + routing)
D-024:    5064f82
M6:       01d6937
M7:       0e074bb (a), de7e3cb (b)
M8:       3da36ea
M9:       fc90f3c
M10:      ee21f0f
M11:      286b566
M12:      add5489
M13:      6a4aedd

## Notes for the next PM

v1 is sealed pending user-side compile + run. Open a new session only
when:
- User returns with validation results (or bugs found during real
  compile), or
- User asks for v1.5 work (pick from FUTURE_FEATURES.md), or
- User requests a content / scene refresh (new personas, new ruleset,
  etc.).

First action on wake-up: read PROJECT_STATE.md, then check PENDING_
QUESTIONS.md, then run the canary test suites on `main` branch to
catch any regressions.
